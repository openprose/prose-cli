{
  "schema": "openprose.runner-case/1",
  "id": "core.openprose-hosted-unavailable",
  "summary": "The OpenProse-billed default fails actionably and never falls back.",
  "phase": 1,
  "tags": ["default", "billing", "no-fallback", "error"],
  "controls": {
    "fixedInvocationId": "fixture-invocation-0001",
    "fixedTimestamp": "2025-01-01T00:00:00Z",
    "fixedMonotonicMs": 0,
    "network": "denied"
  },
  "invocation": {
    "argv": ["--output", "json", "run", "fixture.prose.md"],
    "cwd": "{{WORKSPACE}}",
    "environment": {}
  },
  "expected": {
    "exitCode": 10,
    "stdout": { "kind": "json", "schema": "openprose.runner-result/1" },
    "stderr": { "kind": "empty" },
    "startedHarness": false,
    "errorCode": "HOSTED_UNAVAILABLE",
    "errorAction": "To use the hosted service, run `cli run submit FILE --preview`; running programs on this machine needs a local harness (`cli harness list`)."
  }
}
