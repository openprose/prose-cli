{
  "id": "job-rotate-secret-confirmation-required",
  "feature": "jobs",
  "operation": "job.rotate-secret",
  "description": "rotate-secret without --yes sends nothing (the old secret stays valid).",
  "argv": [
    "--output",
    "json",
    "cli",
    "job",
    "rotate-secret",
    "3c1a9e57-0b4d-4f2a-9e61-5d7b2c8a4f10"
  ],
  "fixture": {
    "environment": "production",
    "credentials": {
      "production": "rr_test_0123456789abcdef0123456789abcdef"
    },
    "storeAvailable": true,
    "exchanges": []
  },
  "exitCode": 2,
  "stdout": {
    "json": {
      "interaction": "job.rotate_secret",
      "operation": "job.rotate-secret",
      "problem": {
        "action": "Rerun the same command with --yes to confirm, or with --preview to see what it would do without sending anything.",
        "boundary": "invocation",
        "code": "CONFIRMATION_REQUIRED",
        "details": {
          "confirmArgv": [
            "--output",
            "json",
            "cli",
            "job",
            "rotate-secret",
            "3c1a9e57-0b4d-4f2a-9e61-5d7b2c8a4f10",
            "--yes"
          ],
          "plannedRequest": {
            "bodyBytes": null,
            "bodySha256": null,
            "description": "Rotate a webhook job's signing secret.",
            "effect": "write",
            "method": "POST",
            "operation": "job.rotate-secret"
          },
          "previewArgv": [
            "--output",
            "json",
            "cli",
            "job",
            "rotate-secret",
            "3c1a9e57-0b4d-4f2a-9e61-5d7b2c8a4f10",
            "--preview"
          ],
          "reason": "--yes is required because it invalidates the current signing secret now; deliveries signed with the old secret are refused until the sender uses the new one"
        },
        "exitCode": 2,
        "message": "This service operation requires explicit confirmation.",
        "retryable": false,
        "schema": "openprose.runner-error/1"
      },
      "result": null,
      "schema": "openprose.service-operation/1"
    }
  }
}
