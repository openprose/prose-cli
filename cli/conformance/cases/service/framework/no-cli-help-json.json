{
  "id": "no-cli-help-json",
  "feature": "framework",
  "operation": "service.operations",
  "description": "`prose --output json help` under the default hosted harness prints the INVOCATION_INVALID envelope with `prose --help`.",
  "argv": [
    "--output",
    "json",
    "help"
  ],
  "fixture": {
    "environment": "production",
    "credentials": {
      "production": null
    },
    "storeAvailable": true,
    "exchanges": []
  },
  "exitCode": 2,
  "stdout": {
    "json": {
      "interaction": null,
      "operation": "cli",
      "problem": {
        "action": "Show the runner help: `prose --output json --help`.",
        "boundary": "invocation",
        "code": "INVOCATION_INVALID",
        "details": {
          "reason": "`help` is a language command, which runs only with a local harness; did you mean `prose --output json --help`? Nothing was forwarded or sent. To run the language command, select a local harness with `prose cli harness use <id>`",
          "suggestedArgv": [
            "--output",
            "json",
            "--help"
          ]
        },
        "exitCode": 2,
        "message": "That command isn't quite right.",
        "retryable": false,
        "schema": "openprose.runner-error/1"
      },
      "result": null,
      "schema": "openprose.service-operation/1"
    }
  }
}
