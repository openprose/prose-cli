{
  "id": "no-cli-examples-list-human",
  "feature": "framework",
  "operation": "example.list",
  "description": "`prose examples list` is a language command, but the default hosted harness runs none, so it exits 2 with `prose cli example list`; nothing is forwarded.",
  "argv": [
    "examples",
    "list"
  ],
  "fixture": {
    "environment": "production",
    "credentials": {
      "production": null
    },
    "storeAvailable": true,
    "exchanges": []
  },
  "exitCode": 2,
  "stdout": {
    "empty": true
  },
  "stderr": {
    "text": "OpenProse: INVOCATION_INVALID: That command isn't quite right.\nDetail: `examples list` is a language command, which runs only with a local harness; did you mean `prose cli example list`? Nothing was forwarded or sent. To run the language command, select a local harness with `prose cli harness use <id>`\nAction: Use `cli example list`: `prose cli example list`.\n"
  }
}
