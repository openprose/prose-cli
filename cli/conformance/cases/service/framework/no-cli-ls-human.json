{
  "id": "no-cli-ls-human",
  "feature": "framework",
  "operation": "run.list",
  "description": "`prose ls` (no `cli`) exits 2 with `cli run list`; nothing is forwarded or billed.",
  "argv": [
    "ls"
  ],
  "fixture": {
    "environment": "production",
    "credentials": {
      "production": null
    },
    "storeAvailable": true,
    "exchanges": []
  },
  "exitCode": 2,
  "stdout": {
    "empty": true
  },
  "stderr": {
    "text": "OpenProse: INVOCATION_INVALID: Unknown command.\nDetail: `ls` is not a command; did you mean `prose cli run list`? Nothing was forwarded or sent. To pass these words to the OpenProse language instead, put `--` before them\nAction: Use `cli run list`: `prose cli run list`.\n"
  }
}
