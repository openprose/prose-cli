{
  "id": "suggestion-settled-missing-argument",
  "feature": "framework",
  "operation": "run.show",
  "description": "A verb correction the grammar would still reject (`cli run show` without RUN_ID) is settled: the suggestion is the next fix, the run listing.",
  "argv": [
    "--output",
    "json",
    "cli",
    "run",
    "shw"
  ],
  "fixture": {
    "environment": "production",
    "credentials": {
      "production": "rr_test_0123456789abcdef0123456789abcdef"
    },
    "storeAvailable": true,
    "exchanges": []
  },
  "exitCode": 2,
  "stdout": {
    "json": {
      "interaction": null,
      "operation": "cli",
      "problem": {
        "action": "Use `cli run show`, then pass <RUN_ID>; list runs with `prose --output json cli run list`.",
        "boundary": "invocation",
        "code": "INVOCATION_INVALID",
        "details": {
          "reason": "unknown command `cli run shw`; did you mean `cli run show`? Commands: cancel, download, input, list, quote, share, show, submit, watch",
          "suggestedArgv": [
            "--output",
            "json",
            "cli",
            "run",
            "list"
          ]
        },
        "exitCode": 2,
        "message": "Unknown command.",
        "retryable": false,
        "schema": "openprose.runner-error/1"
      },
      "result": null,
      "schema": "openprose.service-operation/1"
    }
  }
}
