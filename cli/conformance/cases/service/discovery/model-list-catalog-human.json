{
  "id": "model-list-catalog-human",
  "feature": "discovery",
  "operation": "model.list",
  "description": "With a /models catalog, human `model list` prints the models this account can run (default marked), the premium models any wallet top-up unlocks with their summary and a top-up preview, and the older ids still accepted with the model to use instead. No model id is built in: every line comes from the response.",
  "argv": [
    "cli",
    "model",
    "list"
  ],
  "fixture": {
    "environment": "production",
    "credentials": {
      "production": "rr_test_0123456789abcdef0123456789abcdef"
    },
    "storeAvailable": true,
    "exchanges": [
      {
        "method": "GET",
        "path": "/models",
        "requestHeaders": {
          "Authorization": "Bearer rr_test_0123456789abcdef0123456789abcdef",
          "X-OpenProse-Client": {
            "pattern": "^cli/[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?\\+(rust|bun)$"
          },
          "Accept": "application/json"
        },
        "status": 200,
        "body": {
          "models": [
            "model-sol",
            "model-luna"
          ],
          "default_model": "model-sol",
          "catalog": [
            {
              "id": "model-astra",
              "status": "paid_top_up",
              "tier": "premium",
              "summary": "Most capable model for hard, long tasks. Higher rate per run. Unlocks with any wallet top-up."
            },
            {
              "id": "model-sol",
              "status": "available"
            },
            {
              "id": "model-luna",
              "status": "available"
            },
            {
              "id": "model-sol-legacy",
              "status": "hidden",
              "successor": "model-sol"
            }
          ]
        }
      }
    ]
  },
  "exitCode": 0,
  "stdout": {
    "text": "Available:\n  model-sol (default)\n  model-luna\nPremium — unlocks with any wallet top-up:\n  model-astra: Most capable model for hard, long tasks. Higher rate per run. Unlocks with any wallet top-up.\nNext: prose cli wallet topup --amount-cents 500 --preview\nAlso accepted (not recommended):\n  model-sol-legacy → use model-sol\nNext: prose cli run quote\n"
  }
}
